name: Daily GivenEnergy report

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:
    inputs:
      backfill_days:
        description: 'One-time backfill: days of history to pull, ignoring the usual high-water mark'
        required: false
        default: ''

permissions:
  contents: write

concurrency:
  group: reporter-writes
  cancel-in-progress: false

jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Fetch and append new energy-flow readings
        env:
          GIVENERGY_BEARER_TOKEN: ${{ secrets.GIVENERGY_BEARER }}
          GIVENERGY_BACKFILL_DAYS: ${{ github.event.inputs.backfill_days }}
        run: python GivenEnergy/scripts/fetch_and_append.py

      - name: Fetch Octopus export rates
        env:
          GIVENERGY_BACKFILL_DAYS: ${{ github.event.inputs.backfill_days }}
        run: python GivenEnergy/scripts/fetch_octopus_rates.py

      # NEW: import (Flexible Octopus / VAR, region G) rate history. Full
      # refresh each run - no backfill env var needed, the script always
      # fetches the entire (dozen-row) history so closed-off valid_to values
      # self-correct. Public endpoint, no secrets.
      - name: Fetch Octopus import rates
        run: python GivenEnergy/scripts/fetch_octopus_import_rates.py

      - name: Generate daily report
        run: python GivenEnergy/scripts/generate_report.py

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if [ -f GivenEnergy/data/history.jsonl ]; then git add GivenEnergy/data/history.jsonl; fi
          if [ -f GivenEnergy/data/octopus_export_rates.jsonl ]; then git add GivenEnergy/data/octopus_export_rates.jsonl; fi
          # NEW: without this line the import cache is fetched but never committed,
          # and the weekly workflow (which only reads committed data) never sees it.
          if [ -f GivenEnergy/data/octopus_import_rates.jsonl ]; then git add GivenEnergy/data/octopus_import_rates.jsonl; fi
          if [ -d GivenEnergy/reports ]; then git add GivenEnergy/reports/; fi
          git diff --cached --quiet || git commit -m "Daily GivenEnergy report $(date -u +%Y-%m-%d)"
          git pull --rebase origin main
          git push
